@model CesiZen.Data.Models.StressQuestionModel

@{
    ViewData["Title"] = "Modifier la question";
    var eventId = ViewBag.EventId as int?;
}

<h2>@ViewData["Title"]</h2>

<form asp-action="EditQuestion" method="post" class="needs-validation" novalidate>
    <input type="hidden" asp-for="Id" />
    <input type="hidden" name="eventId" value="@eventId" />

    <div class="mb-3">
        <label asp-for="Title" class="form-label">Titre de la question</label>
        <input asp-for="Title" class="form-control" required />
        <span asp-validation-for="Title" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="Point" class="form-label">Points associés</label>
        <input asp-for="Point" type="number" class="form-control" min="0" required />
        <span asp-validation-for="Point" class="text-danger"></span>
    </div>

    <button type="submit" class="btn btn-primary">
        <i class="bi bi-save me-1"></i> Sauvegarder
    </button>
    <a asp-action="EditEvent" asp-route-id="@eventId" class="btn btn-secondary ms-2">Annuler</a>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
